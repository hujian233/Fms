<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.school.fms.dao.UserDao">

    <resultMap id="UserMap" type="User">
        <id property="id" column="id"/>
        <result property="jobNumber" column="jobnumber"/>
        <result property="userName" column="username"/>
        <result property="password" column="password"/>
        <result property="mailAddress" column="mailaddress"/>
        <result property="authority" column="authority"/>
        <result property="department" column="department"/>
        <result property="loginTime" column="logintime"/>
    </resultMap>


    <select id="selectUser" resultMap="UserMap">
        SELECT
        id,jobnumber,username,password,mailaddress,authority,department,logintime
        FROM
        user
        WHERE 1 = 1
        <if test="id != null">
            AND id = #{id}
        </if>
        <if test="username != null">
            AND username = #{username}
        </if>
        <if test="email != null">
            AND mailaddress = #{mailaddress}
        </if>
    </select>

    <insert id="addUser">
        INSERT INTO user(id, jobnumber, username, password, mailaddress, authority, department, logintime)
        VALUES (#{id}, #{jobnumber}, #{username}, #{password}, #{mailaddress}, #{authority}, #{department},
                #{logintime})
    </insert>

    <update id="changePassword">
        UPDATE user
        SET password = #{newPwd}
        WHERE username = #{username}
    </update>

</mapper>